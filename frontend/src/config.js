// Define your API base URLs here
export const LOCAL_URL = 'http://localhost:5000';
export const DEPLOYED_URL = 'https://quiz-game-z2sm.onrender.com';

// Toggle this variable to switch between local and deployed backends
// Currently set to use the DEPLOYED backend as requested.
export const API_BASE_URL = DEPLOYED_URL;
